import SwiftUI
import AVFoundation

struct MainView: View {
    var body: some View {
        VStack {
            
        }
    }
}

struct MainView_Previews: PreviewProvider {
    static var previews: some View {
        MainView()
    }
}
